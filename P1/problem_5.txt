In this problem I created a singly linked list with the ability to print out the whole chain. The time complexity for this problem is O(n) for the while loops
because the chain can be any length of values. The space complexity of this solution is O(n) because
it is a singly linked which will take up as much space in memory and n number of items it has.