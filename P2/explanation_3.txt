Time complexity is O(n log n) with one while loop and two single for loops. A mergesort has a space complexity of O(n).
This solution uses a variation of merge sort algorithm but instead of merging back to the same list, I use separate lists.