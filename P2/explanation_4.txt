In this problem a divide an conquer algorithm of quicksort is used. The array is split into lo, mid, and high sections that switch with each other. This algorithim is O(n log n)
time complexity with only one while loop. And a memory complexity of O(1).